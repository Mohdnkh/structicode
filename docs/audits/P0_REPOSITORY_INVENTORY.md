# P0 Repository Inventory

## Git and preservation baseline

- Repository identity: Mohdnkh/structicode, cloned locally from https://github.com/Mohdnkh/structicode.
- Original Git branch in the fresh clone: main, clean at b3801df62fdf1ba0ce61515e1ec5c29d28cacc32.
- P0 working branch: codex/48h-enterprise-remediation, created locally from that SHA.
- Existing desktop source snapshot: C:/Users/MOHAMMED/Desktop/structicode-main. It had no .git directory, so no branch, commit, or tracked/untracked status could be inferred from it. It was left untouched.
- The snapshot and clone each contain 81 non-generated source/configuration files after excluding node_modules, dist, __pycache__, and .git. Their contents match after CRLF/LF normalization. Generated artifacts were not compared. The new clone had no pre-existing uncommitted or untracked user work.

## Repository structure at baseline

| Area | Observed contents |
| --- | --- |
| Root | .gitignore, .dockerignore, Dockerfile, package.json, railway.json, backend/, frontend/. No README or docs directory at baseline. |
| Frontend | React/Vite application in frontend/src with App.jsx, main.jsx, index.css, i18n.js; pages Home, Analyzer, StructureDesigner; element forms and a UI button; English/Arabic translation JSON; public images/icons; local steel-section JSON. |
| Backend | FastAPI app in backend/api/main.py; engine/structure_router.py, structure_analyzer.py, load_combination.py, code_router.py, seismic_router.py; concrete and steel calculation modules; code-handler modules; seismic classes; PDF generator and font assets. |
| Dependency manifests | Root package.json, frontend/package.json, backend/requirements.txt. Root and frontend duplicate React-related dependencies with different ranges, including react-router-dom 7 at root and 6 in frontend. |
| JavaScript package-manager files | frontend/package-lock.json and frontend/yarn.lock are both tracked. No root package-lock.json or root yarn.lock. A generated frontend/node_modules/.package-lock.json is also tracked. |
| Python dependency manifest | backend/requirements.txt pins several packages, including FastAPI 0.116.1 and fpdf 1.7.2, while leaving others unpinned, including NumPy, psycopg2-binary, and email-validator. |
| Docker | Dockerfile installs Python requirements and copies the entire context; no frontend build stage. .dockerignore excludes node_modules and package-lock files but retains frontend/dist. |
| Railway | railway.json exists and declares docker build/run commands. It was inspected only and not changed or executed. |
| Generated build output | frontend/dist is tracked. It contains 13 files totaling 4,553,594 bytes, including built JS/CSS and copied public assets. |
| Generated reports | backend/reports/report.pdf is tracked, 28,122 bytes. |
| Tests and CI | No first-party test file, test directory, pytest configuration, package test script, or CI configuration was found. Apparent test/spec names inside node_modules are third-party dependencies. |
| Database and migrations | No database application module, model/migration directory, Alembic configuration, or schema file was found. SQLAlchemy and psycopg2-binary in requirements are dependencies, not evidence of implementation. |
| Environment configuration | No .env.example or other environment template was found. .gitignore ignores local .env variants. |
| Other temporary assets | frontend/src/pages/Untitled-1.txt is tracked. Two FPDF font cache .pkl files are tracked. A DejaVuSans.ttf asset is tracked and is approximately 23.45 MB; its purpose and licensing should be reviewed before changing it. |

## Tracked artifact counts

Git lists 6,941 tracked files at the baseline commit.

| Category | Tracked files | Approximate bytes | P1 recommendation |
| --- | ---: | ---: | --- |
| frontend/node_modules | 6,798 | 80,748,823 | Remove from version control after a reproducible install and lockfile decision; keep ignored. |
| frontend/dist | 13 | 4,553,594 | Stop depending on checked-in build output; build it reproducibly before untracking. |
| Python __pycache__ / .pyc | 49 | 145,194 | Untrack generated bytecode; the current ignore rule alone does not remove already tracked files. |
| backend/reports/report.pdf | 1 | 28,122 | Untrack generated report after preserving any needed audit copy. |
| FPDF .pkl caches | 2 | 170,943 | Determine whether they are regenerated; untrack if reproducible. |
| DejaVuSans.ttf | 1 | 23,449,860 | Review necessity, provenance, license, packaging, and size; do not delete blindly. |
| frontend/src/pages/Untitled-1.txt | 1 | Small | Inspect purpose and remove only after confirming it is unused. |

The steel-section dataset appears at frontend/src/data/steel_sections_data.json and backend/api/engine/data/steel_sections_data.json. Both copies are 3,268 bytes and byte-identical at the baseline SHA. They have no explicit source/version metadata and may diverge later; P1/P5 should choose an authoritative source and publish its units and provenance.

## Ignore and build-context observations

.gitignore names node_modules, __pycache__, bytecode, and reports, but those files remain tracked. Its dist ignore entry is commented out. .dockerignore excludes node_modules and package-lock files, does not exclude frontend/dist, and the Dockerfile does not rebuild frontend/dist. Consequently the image can depend on stale committed output. No tracked artifacts were removed in P0.

## Local-only control

P0 through P11 are local-development phases. Railway, production infrastructure, hosting providers, preview environments, and production operations must remain untouched. P12 is the sole deployment decision phase and requires explicit approval before any deployment. This inventory is documentation only; it does not authorize cleanup, deployment, commit, or push.
